# AI 探索雷达测试报告

**测试日期：** 2026-07-12  
**测试范围：** Dashboard、Lab 页面与设置、系统管理员控制台、数据源管理、雷达运行记录、语言与主题切换、Snowy AI 对话、接口、数据库及前端构建。  
**测试环境：** 本地开发环境；前端 `127.0.0.1:3000`，API `127.0.0.1:8000`，SQLite 预览数据库。

## 1. 总体结论

核心页面和主要业务链路可以正常打开，Lab 设置、关注对象增删、Snowy AI 对话、数据源推荐选择等功能均完成了人工点测。自动化检查也通过：后端 **27 passed**，前端生产构建通过，TypeScript 检查通过，SQLite 完整性检查通过。

当前不建议直接宣称“生产稳定”，主要原因是：雷达运行仍会受到 Semantic Scholar 限流和外部连接重置影响；控制台顶部的最新运行状态与运行记录存在不一致；英文语言切换仍有大量中文残留；数据源推荐在当前数据状态下只能返回 1 条候选，“换一批”不可用。

## 2. 自动化与基础检查

| 编号 | 检查项 | 结果 | 证据 |
|---|---|---|---|
| AUTO-001 | API 测试 | 通过 | `27 passed, 4 warnings` |
| AUTO-002 | 前端生产构建 | 通过 | Next.js build 完成，所有路由生成成功 |
| AUTO-003 | 前端 TypeScript 检查 | 通过 | `npm test` 通过；首次直接执行时因 `.next/types` 尚未生成而失败，构建后恢复正常 |
| AUTO-004 | Git diff 格式检查 | 通过 | `git diff --check` 无输出 |
| AUTO-005 | SQLite 完整性 | 通过 | `PRAGMA integrity_check` 返回 `ok` |
| AUTO-006 | API 基础接口 | 通过 | `/api/version`、`/api/ai/status`、`/api/changes/week` 返回 200 |

## 3. 人工功能测试

| 编号 | 功能 | 结果 | 说明 |
|---|---|---|---|
| UI-001 | Dashboard 首页加载 | 通过 | 热点、分类卡片、本周/本月区域和导航正常渲染 |
| UI-002 | 顶部导航与管理员入口 | 通过 | Dashboard、My Lab、管理控制台入口可用 |
| UI-003 | 中文语言 | 通过 | 页面可正常使用中文 |
| UI-004 | 英文语言切换 | 部分通过 | 分类卡片等部分文字变英文，但“本周热点”、分类说明、空状态等仍为中文 |
| UI-005 | 深色/亮色主题切换 | 通过 | 深色主题可切换并正确渲染，测试结束已恢复亮色 |
| LAB-001 | My Lab 页面 | 通过 | 变化列表、文章卡片、详情链接、Snowy 入口正常 |
| LAB-002 | Lab 设置页面 | 通过 | 关注对象、成员权限、邀请和审计区域可见；数据源统一由系统维护 |
| LAB-003 | 关注对象新增/删除 | 通过 | 新增临时对象“测试对象”后删除成功，数据已恢复 |
| AI-001 | Snowy 初始回答 | 通过 | Dify/DeepSeek 配置生效，能够生成 Lab 范围内分析 |
| AI-002 | Snowy 提问与回答 | 通过 | 发送 HBM 风险问题后成功返回回答，输入框可继续使用 |
| ADMIN-001 | 管理控制台 | 通过 | Lab 目录、数据源统计、自动化配置、运行历史均可见 |
| ADMIN-002 | 数据源目录 | 通过 | 374 条来源可展示，包含论文、新闻、专利、会议、企业动态和 AI 搜索类型 |
| ADMIN-003 | 数据源推荐弹窗 | 通过 | 可打开推荐弹窗、选择候选来源，选择后“加入”按钮启用 |
| ADMIN-004 | 推荐换一批 | 未通过 | 当前只返回 1 条候选，`换一批`按钮为禁用状态，无法轮换推荐结果 |
| RADAR-001 | 雷达运行记录 | 部分通过 | 历史记录可展示进度和错误；已有运行能生成 11/45/51 条变化 |
| RADAR-002 | 最新运行状态一致性 | 未通过 | 控制台顶部显示 `Processing`，但最新运行记录已显示“部分完成”，状态未正确同步 |
| DATA-001 | Dashboard 数据展示 | 部分通过 | 已有变化可在 Lab 和变化页面展示，但部分分类仍为 0，取决于数据源实际产出 |

## 4. 数据完备性检查

当前数据库统计：

| 数据项 | 数量 |
|---|---:|
| 全局数据源 | 374 |
| 已启用数据源 | 40 |
| 文档 | 447 |
| Changes | 113 |
| AI 分析记录 | 326 |
| Lab | 1 |

数据源类型覆盖情况：论文 API 206、新闻订阅 56、论文订阅 54、专利 API 52、企业新闻 3、会议 1、企业产品 1、AI Web Search 1。说明目录层面已经覆盖论文、专利、会议和企业信息，但运行时仍有大量来源没有产出数据，管理员页面也显示只有少数来源已有资料。

## 5. 已发现问题

### P1/P2：最新运行状态与历史记录不一致

控制台顶部的 Latest Run 显示为 `Processing`，而最新运行记录已经是“部分完成”，数据库最新记录的 `phase` 为 `completed`。这会让用户误以为任务仍在运行，也可能导致运行按钮状态和页面刷新行为异常。

**建议：** 页面加载和轮询时以最新 `radar_run` 的终态为准；`completed_with_errors`、`failed`、`completed` 都应停止 Processing 状态，并在前端统一映射状态文案。

### P2：英文语言切换不完整

切换到 English 后，部分导航和卡片已经翻译，但标题、分类说明、空状态、部分统计文案仍然是中文，属于混合语言界面。

**建议：** 将 Dashboard、Lab、管理控制台、运行记录、数据源管理和 Snowy 页面中的可见文案统一纳入翻译字典，并增加中/英/日三种语言的冒烟测试。

### P2：推荐结果不足，“换一批”不可用

推荐弹窗声明每批展示 9 条，但当前实际只有 1 条候选，且“换一批”按钮禁用。对于已经加入大量来源的 Lab，这会让推荐功能看起来像“没有结果”。

**建议：** 推荐接口应区分“已加入”“已停用”“未加入”；候选不足时提供原因和可扩展候选池，换一批应在仍存在未展示候选时保持可用，推荐数量不足时显示明确空状态。

### P2：外部来源限流导致运行部分完成

运行记录中多次出现 `Semantic Scholar ... HTTP Error 429`，并出现分析阶段连接重置；历史运行因此为“部分完成”或失败。现有数据并非完全没有生成，但稳定性和来源覆盖受影响。

**建议：** 对 Semantic Scholar 增加速率限制、指数退避、重试上限和熔断；单个来源失败不应阻塞分析阶段；把来源级错误、分析级错误和超时分开统计，并允许下次运行跳过冷却中的来源。

### P3：缺少标准健康检查接口

`/api/health` 返回 404。若部署平台、反向代理或监控系统依赖该路径，会无法判断 API 是否存活。

**建议：** 增加 `/api/health`，至少检查 API 进程、数据库连接和关键配置状态，并区分 liveness 与 readiness。

### P3：前端类型测试依赖先生成 `.next/types`

在未执行构建的干净状态下直接运行 `npm test` 会因为 `.next/types` 不存在而失败；先构建后再运行则通过。

**建议：** 将 typegen 纳入 test 前置步骤，或调整 TypeScript include，避免测试结果依赖本地是否已有构建产物。

## 6. 后续回归优先级

1. 先修复运行状态终态同步和轮询逻辑，这是用户最容易直接感知的问题。
2. 隔离 Semantic Scholar 等易限流来源，验证连续多次运行不会因为单个来源拖垮全局任务。
3. 完善翻译字典并补充语言切换回归用例。
4. 修复推荐候选池和换一批交互，再验证选择、批量加入、重复加入和停用来源场景。
5. 增加 `/api/health` 与干净环境下的一键测试命令。

## 7. 测试边界

本轮使用现有登录会话完成页面点测；未执行退出登录、删除真实 Lab、删除真实数据源等破坏性操作，也没有再次触发完整的大规模雷达运行，以避免改变现有数据或重复消耗外部接口额度。雷达运行部分结合现有运行历史、数据库记录和页面状态进行验证。

