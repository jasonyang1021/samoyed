# AI 探索雷达第二轮修复与体验测试报告

**测试日期：** 2026-07-12  
**测试目标：** 修复上一轮测试发现的问题，并补充真实使用体验评价。

## 1. 修复结果

| 问题 | 修复内容 | 回归结果 |
|---|---|---|
| 控制台一直显示 Processing | 最新运行状态按 `running / completed / completed_with_errors / failed` 正确映射；新增 KPI 实时轮询 | 通过，最新历史为部分完成时显示 `Partial` |
| 运行结束后页面状态不及时 | 管理员 KPI 和运行面板均每 3 秒刷新，终态自动解除运行锁定 | 通过 |
| 英文页面中仍混有中文 | 补充 Dashboard 标题、分类说明、空状态和数量文案 | 通过，英文标题、分类说明、空状态均已切换 |
| 推荐“换一批”看起来无响应 | 增加当前批次/候选总数提示，并用 disabled 状态和 title 说明“候选不足一批” | 通过，当前页面能明确显示候选数量和按钮原因 |
| 外部来源 429 错误信息为空 | 对 429/5xx 增加有限退避重试，并返回 `HTTP 429 from host` 等可读错误 | 自动化通过；完整外部运行仍需持续观察 |
| 缺少 `/api/health` | 增加 `/api/health`，并验证前端代理路径 | 通过，API 与前端代理均返回 `{"status":"ok"}` |

## 2. 第二轮自动化结果

- 后端 pytest：**28 passed, 4 warnings**。
- 前端 `npm run build`：通过，所有 App Router 路由构建成功。
- 前端 `npm test`：通过，TypeScript 检查通过。
- `git diff --check`：通过。
- SQLite `integrity_check`：返回 `ok`。

警告仍为 FastAPI `on_event` 弃用提示，不影响当前功能，但建议后续迁移到 lifespan。

## 3. 页面回归与体验评价

### Dashboard：4/5

优点：入口清晰、分类卡片容易理解、周/月数据区分合理，主题切换直观。英文切换后的标题、分类说明、空状态已经保持一致。

体验问题：页面仍有较多信息密度较高的卡片和状态文案；在数据量较大时，用户需要依赖分类点击和滚动来定位重点。建议增加“有新变化/高重要性/数据源类型”筛选。

### Lab 页面：4/5

优点：Lab 研究内容、变化、关注对象和 Snowy 入口关系清楚；文章卡片可以进入详情。

体验问题：不同来源的内容完整度差异明显，有些只有摘要或元数据。建议在卡片上直接显示“正文/摘要/元数据”标识，避免用户误以为所有条目都包含完整原文。

### Lab 设置：4.5/5

优点：数据源由系统管理员统一维护、Lab 只负责使用的权限边界已经体现；关注对象新增和删除反馈明确。

体验问题：Lab 管理员、Lab 用户和系统管理员的操作范围仍需要在首次进入时用一段简短说明提示，减少权限疑惑。

### 数据源推荐：3.5/5

优点：推荐弹窗较大，支持勾选和批量加入；本轮增加了“当前显示几条/共几条可选”的反馈。

体验问题：当 Lab 已经加入绝大多数候选时，只剩 1 条来源，用户仍可能期待“换一批”能产生新结果。后续应支持“扩大候选池”“显示已加入来源”“按论文/专利/会议/企业动态补齐推荐”等操作，而不只是禁用按钮。

### 雷达运行：3/5

优点：有运行中禁用、来源进度、当前来源和错误记录；本轮终态映射已经修复。

体验问题：运行是长任务，用户仍需要等待较长时间才能看到最终结果；外部来源限流会造成“部分完成”，用户需要理解错误是否影响整体结果。建议增加成功率、跳过来源数量、预计剩余时间和“重新运行失败来源”按钮。

### Snowy AI：4/5

优点：本轮验证能够打开助手、显示回答状态、保持输入框可用；Dify/DeepSeek 配置生效，回答带有“仅使用 Lab 数据”的边界说明。

体验问题：首次回答等待期间缺少更明确的阶段提示；回答较长时，来源证据和原文链接不够突出。建议将“正在读取 Lab 数据 / 正在调用 Dify / 正在整理结论”做成轻量步骤，并把引用来源固定到回答底部。

### 语言与主题：4/5

优点：中文、英文、亮色、深色均可切换，第二轮重点补齐了 Dashboard 中上一轮发现的混合语言问题。

体验问题：当前语言机制仍是运行时文本替换，复杂页面和后续新增文案容易漏翻；建议逐步迁移为组件级字典，而不是依赖 DOM 文本扫描。

## 4. 剩余风险

1. Semantic Scholar 等公共 API 仍可能 429；本轮改进了重试和错误可读性，但没有伪造外部成功，必须通过真实连续运行观察限流恢复效果。
2. 数据源目录数量多，但启用来源和实际有数据来源仍明显少于目录总数；数据完备性取决于来源健康、API Key 和上游发布时间格式。
3. 当前浏览器回归没有再次触发完整大规模雷达运行，以避免重复消耗外部接口额度；运行终态使用已有运行记录和实时控制台状态验证。

## 5. 综合评价

第二轮修复后，系统从“核心功能可用但状态反馈和语言体验不稳定”提升到“主流程可用，关键状态可解释，主要页面可连续使用”。综合体验建议评分：**3.9/5**。

在正式开源或面向更多用户前，优先级最高的后续工作是：来源失败重跑、来源健康度面板、推荐候选池扩展、组件级国际化，以及真实环境下连续多轮雷达运行压测。

